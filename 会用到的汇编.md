## 段/符号相关（汇编指示符 directives）

* `.data`
* `.text`
* `.globl`

## 访存与地址构造

* `lw`
* `sw`
* `lui`
* `auipc`  （通常用于把 `la` 展开成 `auipc + addi` 这种 PC-relative 形式）

## 算术/逻辑

* `add`
* `addi`
* `sub`
* `and`
* `or`
* `xor`
* `xori`

## 移位

* `sll`
* `slli`
* `sra`

## 比较/置位

* `slt`
* `sltu`
* `sltiu`

## 控制流/调用返回

* `beq`
* `jal`
* `jalr`

## 乘除取余（M 扩展；非 RV32I） 可以不用管()

* `mul`
* `div`
* `rem`

---
后续可改进(没必要):
1. `mul/div/rem`（需要改成调用软实现占位，例如 `jal ra, __mulsi3` 等）；
